export { default } from "./Typography.vue";

export interface ThemeTypography {
  status: {
    primary: string;
    success: string;
    info: string;
    danger: string;
    warning: string;
  };
}
