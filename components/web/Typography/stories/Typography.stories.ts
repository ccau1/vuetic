import { story } from "../../utils";
import TypographyCategory from "./TypographyCategory.story.vue";
import TypographyStatus from "./TypographyStatus.story.vue";

export default {
  title: "Typography"
};

export const typographyCategory = story(TypographyCategory);
export const typographyStatus = story(TypographyStatus);
