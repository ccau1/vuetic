import { story } from "../../utils";
import CardElevation from "./CardElevation.story.vue";
import CardAccessories from "./CardAccessories.story.vue";

export default {
  title: "Card"
};

export const cardElevation = story(CardElevation);
export const cardAccessories = story(CardAccessories);
