export { default } from "./Card.vue";

export interface ThemeCard {
  paddingVertical: string | number;
  paddingHorizontal: string | number;
  cornerRadius: string | number;
  defaultElevation: string | number;
}
