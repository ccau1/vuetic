import { story } from "../../utils";
import CheckboxBasic from "./CheckboxBasic.story.vue";
import CheckboxLineThrough from "./CheckboxLineThrough.story.vue";

export default {
  title: "Checkbox"
};

export const checkboxBasic = story(CheckboxBasic);
export const checkboxLineThrough = story(CheckboxLineThrough);
