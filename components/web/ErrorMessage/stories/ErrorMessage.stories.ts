import { story } from "../../utils";
import ErrorMessageBasic from "./ErrorMessageBasic.story.vue";

export default {
  title: "ErrorMessage"
};

export const errorMessageBasic = story(ErrorMessageBasic);
